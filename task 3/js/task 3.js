//Побудувати ялинку

for (let christmasTree = 0; christmasTree < 3; christmasTree++) {
		for (let circleLines = 1; circleLines <= 5; circleLines++) {
			for (let circleAdding = 1; circleAdding <= circleLines; circleAdding++) {
			document.write(`o`)
			}
	document.write(`<br>`)
}
}