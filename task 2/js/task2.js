//Відобразити трикутник за допомогою символів «о»

for (let lineCircle = 1; lineCircle <= 7; lineCircle++) {
for (let circleAdding = 1; circleAdding <= lineCircle; circleAdding++) {
	document.write(`o`);
}
document.write(`<br>`)
}